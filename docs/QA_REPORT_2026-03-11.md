# TradVue Backend QA Report — Render Migration
**Date:** 2026-03-11  
**Auditor:** Zip (QA Agent, ApexLogics)  
**Backend URL:** `https://tradvue-api.onrender.com`  
**Frontend URL:** `https://www.tradvue.com`  
**Context:** Post-migration audit from Railway → Render

---

## Summary

| Category | Pass | Warn | Fail |
|----------|------|------|------|
| Health | 1 | 0 | 0 |
| Market Data | 4 | 0 | 0 |
| Stocks | 1 | 1 | 1 |
| Calendar | 5 | 0 | 0 |
| News | 0 | 2 | 0 |
| Crypto | 1 | 0 | 0 |
| Tools | 0 | 1 | 2 |
| Infrastructure | 3 | 1 | 0 |
| **TOTAL** | **15** | **5** | **3** |

**3 broken endpoints, 5 data quality warnings. Backend is mostly functional but has missing routes and data gaps.**

---

## Infrastructure

### SSL Certificate
✅ **Valid** — Cert issued by Google Trust Services (WE1), covers `*.onrender.com`  
- **Subject:** `CN=onrender.com`  
- **Valid from:** Jan 28 2026  
- **Expires:** Apr 28 2026 (~48 days remaining)  
- **Protocol:** HTTP/2  
- ⚠️ **Note:** Certificate expires in ~48 days. Render auto-renews, but monitor if auto-renewal has issues.

### CORS
✅ **Correctly configured**  
- `Access-Control-Allow-Origin: https://www.tradvue.com` on all 200 responses  
- `Access-Control-Allow-Credentials: true`  
- Preflight (OPTIONS) returns **204** with:
  - `Access-Control-Allow-Methods: GET,POST,PUT,DELETE,PATCH`
  - `Access-Control-Allow-Headers: Content-Type,Authorization`
  - `Vary: Origin`

### Response Compression
✅ **Gzip active** — All endpoints respond with `Content-Encoding: gzip`  
- Brotli (`br`) also advertised via `Accept-Encoding: br` negotiation  
- Compression confirmed on all 200 and 404 responses  
- Server is Cloudflare-fronted (`cf-ray` header present, `server: cloudflare`)

### Rate Limiting
✅ **Rate limit headers present**  
- `ratelimit-limit: 1000` per 900s window  
- `ratelimit-remaining: 986` (healthy headroom)

---

## Endpoint Tests

### Health

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /health` | 200 | 0.25s | ✅ |

**Response:**
```json
{"status":"OK","timestamp":"2026-03-11T16:37:45.095Z","service":"TradVue API"}
```
✅ Service is up, timestamp is live.

---

### Market Data

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/market-data/batch?symbols=SPY,AAPL,TSLA` | 200 | 0.77s | ✅ |
| `GET /api/market-data/quote/AAPL` | 200 | 0.67s | ✅ |
| `GET /api/market-data/movers` | 200 | 0.30s | ✅ |
| `GET /api/market-data/status` | 200 | 0.92s | ✅ |

**Batch — sample data:**
```
SPY:  $675.88 (-0.19%)  source: finnhub
AAPL: $260.94 (+0.04%)  source: finnhub
TSLA: $408.50 (+2.32%)  source: finnhub
```
✅ Real Finnhub data, live prices, correct fields.

**Quote AAPL:** `$260.94` with high/low/open/prevClose — ✅ Live data.  
**Movers:** Gainers/losers populated with real symbols and prices — ✅  
**Status:** `{"isOpen":true,"session":"regular","timezone":"America/New_York"}` — ✅ Accurate (market was open during test).

---

### Stocks

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/stocks/AAPL/ratings` | 200 | 0.35s | ⚠️ |
| `GET /api/stocks/AAPL/analysis` | 404 | 0.34s | ❌ |

**Ratings — issues:**
```json
{
  "consensus": {"label": null, "mean": null, "key": null},
  "priceTargets": {"high": null, "low": null, "mean": null, "median": null, "currentPrice": null, "upsidePct": null},
  "analystCount": 54,
  "distribution": {"strongBuy": 14, "buy": 22, "hold": 16, "sell": 2, "strongSell": 0}
}
```
⚠️ **Partial data:** Analyst distribution and count are present (from Finnhub), but `consensus.label`, `priceTargets.*`, and `currentPrice` are all `null`. Price target aggregation appears broken — likely a secondary data fetch failure or unmapped field.

❌ **Analysis:** `{"error":"Route not found"}` — This endpoint doesn't exist. If it was available on Railway, it was either not migrated or was removed. Needs investigation.

---

### Calendar

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/calendar/events?from=2026-03-11&to=2026-03-11` | 200 | 0.33s | ✅ |
| `GET /api/calendar/today` | 200 | 0.32s | ✅ |
| `GET /api/calendar/earnings` | 200 | 0.59s | ✅ |
| `GET /api/calendar/upcoming?days=7` | 200 | 0.32s | ✅ |
| `GET /api/calendar/high-impact` | 200 | 0.32s | ✅ |

All calendar endpoints fully functional with rich data:

| Endpoint | Events Returned |
|----------|----------------|
| `/events` (today) | 91 events |
| `/today` | 91 events |
| `/earnings` | 1,482 earnings events |
| `/upcoming?days=7` | 544 events |
| `/high-impact` | 337 high-impact events |

✅ Data from ForexFactory + Finnhub. Fields include `impact`, `country`, `currency`, `epsEstimate`, `epsActual`, `revenueEstimate`, `revenueActual`. Calendar is the strongest part of the backend.

---

### News

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/news?category=general&limit=25` | 200 | 0.28s | ⚠️ |
| `GET /api/news?category=stocks&symbol=AAPL` | 200 | 0.26s | ⚠️ |

**General news:**
```json
{"articles":[],"total":0,"limit":25,"offset":0,"has_more":false}
```
⚠️ **Empty!** Returns 200 but zero articles. News feed for `general` category is not populating. Could be an API key issue, rate limit on the news provider, or a broken fetch job.

**AAPL stocks news:**
```json
{"articles":[{"id":4,"title":"Tech Stocks Rally on AI Investment News","summary":"Technology stocks surge following announcements..."}]}
```
⚠️ **Likely mock/static data.** Only 1 article, generic title, `id: 4` suggests a seeded DB row rather than a live fetch. The news system appears to be returning fallback static content instead of live articles.

---

### Crypto

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/crypto/prices?limit=10` | 200 | 0.26s | ✅ |

**Sample:**
```json
{"id":"bitcoin","symbol":"BTC","name":"Bitcoin","price":67420.50,"change24h":-1.23,"change7d":3.45,"marketCap":1320000000000}
```
✅ 10 cryptocurrencies returned with price, 24h/7d change, market cap, volume. Data from CoinGecko. Looks live.

---

### Tools

| Endpoint | Status | Time | Result |
|----------|--------|------|--------|
| `GET /api/tools/screener` | 200 | 0.27s | ⚠️ |
| `GET /api/tools/heatmap` | 404 | 0.27s | ❌ |
| `GET /api/tools/sectors` | 404 | 0.26s | ❌ |

**Screener — issues:**
```json
{"symbol":"NVDA","name":"NVIDIA Corp.","sector":"Technology","price":0,"change":0,"pe":37.66,"divYield":0,"marketCap":4521.5,"eps":4.9}
```
⚠️ **Missing live prices!** `price: 0` and `change: 0` for ALL 30 stocks. Fundamental data (PE, EPS, market cap) is present, but live price feed is not being joined in. This makes the screener non-functional for users.

❌ **Heatmap:** `{"error":"Route not found"}` — Route missing.  
❌ **Sectors:** `{"error":"Route not found"}` — Route missing.

---

## Issues Summary

### ❌ Critical — Broken Endpoints

| # | Endpoint | Issue |
|---|----------|-------|
| 1 | `GET /api/stocks/AAPL/analysis` | 404 — route not registered |
| 2 | `GET /api/tools/heatmap` | 404 — route not registered |
| 3 | `GET /api/tools/sectors` | 404 — route not registered |

> **Likely cause:** These routes may not have been included in the Render deployment. Check the Express router registration for these paths.

### ⚠️ Warnings — Data Quality

| # | Endpoint | Issue |
|---|----------|-------|
| 4 | `GET /api/stocks/AAPL/ratings` | `consensus.label`, all `priceTargets.*` fields are `null` |
| 5 | `GET /api/news?category=general&limit=25` | Empty articles array — news feed broken |
| 6 | `GET /api/news?category=stocks&symbol=AAPL` | Only 1 static/mock article returned |
| 7 | `GET /api/tools/screener` | `price: 0`, `change: 0` for all 30 stocks — live price join missing |
| 8 | SSL cert | Expires Apr 28 2026 (~48 days) — verify Render auto-renewal is set up |

---

## What's Working Well

- ✅ Server is live and responsive on Render (avg response ~300ms)
- ✅ HTTP/2 + Cloudflare CDN layer active
- ✅ SSL valid, HSTS enabled
- ✅ CORS properly scoped to `https://www.tradvue.com`
- ✅ Gzip/Brotli compression on all responses
- ✅ Rate limiting configured (1000 req/15min)
- ✅ All market data endpoints (batch, quote, movers, status) returning live Finnhub data
- ✅ Calendar system is fully functional (5/5 endpoints, rich data)
- ✅ Crypto prices working with CoinGecko data

---

## Recommended Actions

1. **[HIGH]** Register missing routes: `/api/stocks/AAPL/analysis`, `/api/tools/heatmap`, `/api/tools/sectors`
2. **[HIGH]** Fix screener price feed — `price` and `change` fields returning 0; likely a missing quote join
3. **[HIGH]** Fix general news feed — `articles: []` indicates broken fetch or missing API key for news provider
4. **[MEDIUM]** Fix ratings price targets — `consensus.label` and all `priceTargets.*` null; check Finnhub price target endpoint wiring
5. **[MEDIUM]** Replace static AAPL news article with live feed; the single seeded article is a placeholder
6. **[LOW]** Verify Render auto-renewal for SSL cert (expires Apr 28)

---

*Report generated by Zip QA Agent — ApexLogics*  
*Test timestamp: 2026-03-11T16:37 EDT*
